<Window x:Class="AgentAssistant.IntranetLoginDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.wpfui.com"
        Title="인트라넷 로그인" 
        Height="300" 
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 제목 -->
        <TextBlock Grid.Row="0" 
                   Text="🔐 인트라넷 로그인" 
                   FontSize="20"
                   FontWeight="Bold"
                   Foreground="#2D3436"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>
        
        <!-- 사용자 ID -->
        <TextBlock Grid.Row="1" 
                   Text="사용자 ID" 
                   FontSize="13"
                   Foreground="#2D3436"
                   Margin="0,0,0,5"/>
        <TextBox Grid.Row="2"
                 x:Name="UsernameInput" 
                 Height="40"
                 Padding="10"
                 FontSize="14"
                 Margin="0,0,0,15"
                 KeyDown="Input_KeyDown"/>
        
        <!-- 비밀번호 -->
        <TextBlock Grid.Row="3" 
                   Text="비밀번호" 
                   FontSize="13"
                   Foreground="#2D3436"
                   Margin="0,0,0,5"/>
        <PasswordBox Grid.Row="4"
                     x:Name="PasswordInput" 
                     Height="40"
                     Padding="10"
                     FontSize="14"
                     Margin="0,0,0,15"
                     KeyDown="Input_KeyDown"/>
        
        <!-- 설명 -->
        <TextBlock Grid.Row="5"
                   Text="⚠️ 본인의 정당한 계정 정보만 입력하세요.&#x0a;자동 로그인 정보는 저장되지 않습니다."
                   FontSize="11"
                   Foreground="#6C757D"
                   TextWrapping="Wrap"
                   VerticalAlignment="Center"
                   Margin="0,10,0,10"/>
        
        <!-- 버튼 -->
        <StackPanel Grid.Row="6" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center"
                    Margin="0,10,0,0">
            <Button x:Name="LoginButton"
                    Content="🔐 로그인" 
                    Width="120" 
                    Height="45"
                    Background="#6C5CE7"
                    Foreground="White"
                    BorderThickness="0"
                    FontWeight="Bold"
                    FontSize="14"
                    Cursor="Hand"
                    IsDefault="True"
                    Click="Login_Click"
                    Margin="0,0,10,0">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                            CornerRadius="5">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                                        VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#5F4FD1"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            
            <Button Content="❌ 취소" 
                    Width="120" 
                    Height="45"
                    Background="#DFE6E9"
                    Foreground="#2D3436"
                    BorderThickness="0"
                    FontSize="14"
                    Cursor="Hand"
                    IsCancel="True"
                    Click="Cancel_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                            CornerRadius="5">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                                        VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#B2BEC3"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>

