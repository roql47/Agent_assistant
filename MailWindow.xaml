<ui:FluentWindow x:Class="AgentAssistant.MailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:AgentAssistant"
        Title="메일 목록" 
        Height="700" 
        Width="950"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round">
    
    <ui:FluentWindow.Resources>
        <local:ReadStatusConverter x:Key="ReadStatusConverter"/>
        <local:ReadColorConverter x:Key="ReadColorConverter"/>
        <local:AttachmentConverter x:Key="AttachmentConverter"/>
    </ui:FluentWindow.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 헤더 -->
        <Border Grid.Row="0" 
                Background="#6C5CE7" 
                Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock x:Name="TitleText"
                               Text="📧 메일 목록" 
                               FontSize="20"
                               FontWeight="Bold"
                               Foreground="White"/>
                    <TextBlock x:Name="MailInfoText"
                               Text="총 0개의 메일"
                               FontSize="12"
                               Foreground="#E0E0E0"
                               Margin="0,5,0,0"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- 메일 목록 -->
        <ScrollViewer Grid.Row="1" 
                      VerticalScrollBarVisibility="Auto"
                      Padding="20">
            <ItemsControl x:Name="MailList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" 
                                Padding="15" 
                                CornerRadius="8"
                                Margin="0,0,0,10"
                                BorderThickness="1"
                                BorderBrush="#E0E0E0">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#F8F9FA"/>
                                            <Setter Property="BorderBrush" Value="#6C5CE7"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <!-- 제목 -->
                                <TextBox Grid.Column="0" Grid.Row="0"
                                         Text="{Binding Subject, Mode=OneWay}" 
                                         FontSize="15"
                                         FontWeight="SemiBold"
                                         Foreground="#2D3436"
                                         TextWrapping="Wrap"
                                         IsReadOnly="True"
                                         BorderThickness="0"
                                         Background="Transparent"
                                         Cursor="IBeam"
                                         Padding="0"
                                         Margin="0,0,10,5"/>
                                
                                <!-- 상태 아이콘 -->
                                <StackPanel Grid.Column="1" Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Top">
                                    <TextBlock Text="{Binding IsRead, Converter={StaticResource ReadStatusConverter}}" 
                                               FontSize="11"
                                               Foreground="{Binding IsRead, Converter={StaticResource ReadColorConverter}}"
                                               FontWeight="Bold"
                                               Margin="0,0,5,0"/>
                                    <TextBlock Text="{Binding HasAttachment, Converter={StaticResource AttachmentConverter}}" 
                                               FontSize="12"
                                               Margin="0,0,10,0"/>
                                </StackPanel>
                                
                                <!-- 날짜/크기 -->
                                <StackPanel Grid.Column="2" Grid.Row="0" VerticalAlignment="Top">
                                    <TextBlock Text="{Binding Date}" 
                                               FontSize="12"
                                               Foreground="#6C757D"/>
                                    <TextBlock Text="{Binding Size}" 
                                               FontSize="10"
                                               Foreground="#ADB5BD"
                                               HorizontalAlignment="Right"/>
                                </StackPanel>
                                
                                <!-- 발신자/수신자 -->
                                <StackPanel Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="3" Orientation="Horizontal" Margin="0,5,0,0">
                                    <TextBlock Text="보낸사람: " FontSize="11" Foreground="#6C757D"/>
                                    <TextBlock Text="{Binding Sender}" FontSize="11" Foreground="#495057" Margin="0,0,15,0"/>
                                    <TextBlock Text="받는사람: " FontSize="11" Foreground="#6C757D"/>
                                    <TextBlock Text="{Binding Receiver}" FontSize="11" Foreground="#495057" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        
        <!-- 하단 페이지네이션 & 버튼 -->
        <Border Grid.Row="2" 
                Background="White"
                BorderThickness="0,1,0,0"
                BorderBrush="#E0E0E0"
                Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- 페이지네이션 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="PrevPageButton"
                            Content="◀ 이전" 
                            Width="80" 
                            Height="35" 
                            Margin="0,0,10,0"
                            Background="#6C5CE7" 
                            Foreground="White" 
                            BorderThickness="0"
                            Click="PrevPage_Click"/>
                    
                    <TextBlock x:Name="PageInfoText" 
                               Text="1 / 1 페이지" 
                               FontSize="14" 
                               VerticalAlignment="Center" 
                               Margin="0,0,10,0"/>
                    
                    <Button x:Name="NextPageButton"
                            Content="다음 ▶" 
                            Width="80" 
                            Height="35"
                            Background="#6C5CE7" 
                            Foreground="White" 
                            BorderThickness="0"
                            Click="NextPage_Click"/>
                </StackPanel>
                
                <!-- 닫기 버튼 -->
                <Button Grid.Column="1"
                        Content="닫기" 
                        Width="100" 
                        Height="40"
                        Background="#DFE6E9"
                        Foreground="#2D3436"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="Close_Click"/>
            </Grid>
        </Border>
    </Grid>
</ui:FluentWindow>

