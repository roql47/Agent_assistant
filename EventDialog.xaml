    <ui:FluentWindow x:Class="AgentAssistant.EventDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="일정 관리" 
        Height="450" 
        Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 헤더 -->
        <TextBlock Grid.Row="0" 
                   x:Name="DateText"
                   Text="2024년 10월 16일" 
                   FontSize="18"
                   FontWeight="Bold"
                   Foreground="#2D3436"
                   Margin="0,0,0,15"/>
        
        <!-- 일정 목록 및 입력 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- 기존 일정 목록 -->
                <Border Background="White" 
                        Padding="15" 
                        CornerRadius="10"
                        Margin="0,0,0,15">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="일정 목록" 
                                   FontSize="14" 
                                   FontWeight="SemiBold"
                                   Foreground="#2D3436"
                                   Margin="0,0,0,10"/>
                        
                        <ItemsControl x:Name="EventListControl">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#F8F9FA" 
                                            Padding="10" 
                                            CornerRadius="5"
                                            Margin="0,0,0,8">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="{Binding Title}" 
                                                          FontWeight="SemiBold"
                                                          FontSize="13"/>
                                                <TextBlock Text="{Binding Time}" 
                                                          Foreground="#6C757D"
                                                          FontSize="11"
                                                          Margin="0,2,0,0"/>
                                                <TextBlock Text="{Binding Description}" 
                                                          Foreground="#6C757D"
                                                          FontSize="11"
                                                          TextWrapping="Wrap"
                                                          Margin="0,2,0,0"/>
                                            </StackPanel>
                                            
                                            <Button Grid.Column="1" 
                                                    Content="🗑️" 
                                                    FontSize="14"
                                                    Width="30" 
                                                    Height="30"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    Cursor="Hand"
                                                    Tag="{Binding}"
                                                    Click="DeleteEvent_Click"
                                                    ToolTip="삭제">
                                                <Button.Style>
                                                    <Style TargetType="Button">
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#FFE6E6"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        
                        <TextBlock x:Name="NoEventsText"
                                   Text="등록된 일정이 없습니다." 
                                   Foreground="#6C757D"
                                   FontSize="12"
                                   HorizontalAlignment="Center"
                                   Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>
                
                <!-- 새 일정 추가 -->
                <Border Background="White" 
                        Padding="15" 
                        CornerRadius="10">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="새 일정 추가" 
                                   FontSize="14" 
                                   FontWeight="SemiBold"
                                   Foreground="#2D3436"
                                   Margin="0,0,0,10"/>
                        
                        <TextBlock Text="제목" FontSize="12" Margin="0,0,0,3"/>
                        <TextBox x:Name="TitleInput" 
                                 Height="35"
                                 Padding="8"
                                 FontSize="13"
                                 Margin="0,0,0,10"/>
                        
                        <TextBlock Text="시간" FontSize="12" Margin="0,0,0,3"/>
                        <TextBox x:Name="TimeInput" 
                                 Height="35"
                                 Padding="8"
                                 FontSize="13"
                                 Margin="0,0,0,10"
                                 Text="오후 2:00"/>
                        
                        <TextBlock Text="메모" FontSize="12" Margin="0,0,0,3"/>
                        <TextBox x:Name="DescriptionInput" 
                                 Height="60"
                                 Padding="8"
                                 FontSize="13"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto"
                                 Margin="0,0,0,10"/>
                        
                        <Button Content="➕ 일정 추가" 
                                Height="44"
                                Background="#6C5CE7"
                                Foreground="White"
                                BorderThickness="0"
                                Click="AddEvent_Click"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
        
        <!-- 하단 버튼 -->
        <Button Grid.Row="2" 
                Content="닫기" 
                Width="100" 
                Height="40"
                Background="#DFE6E9"
                Foreground="#2D3436"
                BorderThickness="0"
                Click="Close_Click"
                HorizontalAlignment="Right"
                Margin="0,15,0,0"/>
    </Grid>
</ui:FluentWindow>

