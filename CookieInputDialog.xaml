<Window x:Class="AgentAssistant.CookieInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.wpfui.com"
        Title="Chrome 쿠키 입력" 
        Height="550" Width="650"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FFF3CD" Padding="15" CornerRadius="8" Margin="0,0,0,10" BorderBrush="#FFC107" BorderThickness="1">
            <TextBlock TextWrapping="Wrap">
                <Run FontWeight="Bold" FontSize="15">⚠️ Session 쿠키는 메모리에만 저장됩니다</Run>
                <LineBreak/><LineBreak/>
                <Run>그룹웨어는 보안상 쿠키를 디스크에 저장하지 않습니다.</Run>
                <LineBreak/>
                <Run FontWeight="Bold" Foreground="#E67E22">세션 만료 시 다시 복사가 필요합니다.</Run>
            </TextBlock>
        </Border>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock TextWrapping="Wrap">
            <Run FontWeight="Bold" FontSize="14">🍪 쿠키 복사 방법 (30초 소요)</Run>
            <LineBreak/><LineBreak/>
            <Run FontWeight="Bold">1.</Run><Run> Chrome에서 인트라넷 로그인</Run>
            <LineBreak/>
            <Run FontWeight="Bold">2.</Run><Run> F12 키 눌러서 개발자 도구 열기</Run>
            <LineBreak/>
            <Run FontWeight="Bold">3.</Run><Run> </Run><Run FontWeight="Bold" Foreground="#6C5CE7">Network</Run><Run> 탭 클릭</Run>
            <LineBreak/>
            <Run FontWeight="Bold">4.</Run><Run> 아무 요청 클릭 → Request Headers에서</Run>
            <LineBreak/>
            <Run>   </Run><Run FontWeight="Bold" Foreground="#E74C3C">"cookie:"</Run><Run> 줄 </Run><Run FontWeight="Bold">전체 복사</Run>
            <LineBreak/>
            <Run FontWeight="Bold">5.</Run><Run> 아래에 </Run><Run FontWeight="Bold" Foreground="#27AE60">Ctrl+V 붙여넣기</Run>
            <LineBreak/><LineBreak/>
            <Run FontSize="11" Background="#E8F5E9" Foreground="#2E7D32">💡 자동 파싱: "cookie:" 헤더, 공백 등이 자동으로 제거되어 저장됩니다!</Run>
        </TextBlock>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <TextBlock TextWrapping="Wrap" Margin="0,0,0,5">
                <Run FontWeight="Bold" FontSize="13">📋 쿠키 문자열 붙여넣기:</Run>
            </TextBlock>
            <Border Background="White" BorderBrush="#6C5CE7" BorderThickness="2" CornerRadius="5" Padding="5">
                <TextBox x:Name="CookieStringInput" 
                         Height="100" 
                         TextWrapping="Wrap" 
                         AcceptsReturn="True" 
                         VerticalScrollBarVisibility="Auto"
                         BorderThickness="0"
                         FontFamily="Consolas"
                         FontSize="11"
                         Background="Transparent"/>
            </Border>
            <TextBlock Margin="0,5,0,0" FontSize="10" Foreground="Gray" TextWrapping="Wrap">
                예: cookie: ASP.NET_SessionId=xxx; Smart2Application=yyy; MainPageLoaded=Y
            </TextBlock>
        </StackPanel>

        <Border Grid.Row="3" Background="#E8F5E9" Padding="10" CornerRadius="5" Margin="0,10,0,10" BorderBrush="#4CAF50" BorderThickness="1">
            <TextBlock FontSize="11" Foreground="#2E7D32" TextWrapping="Wrap">
                ✅ 자동 파싱: 모든 쿠키가 자동으로 추출되어 manual_cookies.json에 저장됩니다!
            </TextBlock>
        </Border>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="저장" Width="80" Height="30" Margin="0,0,10,0" 
                    Click="Save_Click" IsDefault="True"/>
            <Button Content="취소" Width="80" Height="30" 
                    Click="Cancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>

